# Pi 5 전용 (Linux + 하드웨어). PC에서는 설치 불가.
-r requirements.txt

# GPIO / SPI / I2C
gpiozero>=2.0
lgpio>=0.2.2.0
spidev>=3.6
smbus2>=0.4.3

# 디스플레이 / 서보
adafruit-circuitpython-rgb-display>=3.11.0  # ILI9341 / ST7789 / 기타 SPI LCD
pillow>=10.0.0                              # pygame surface → LCD 변환
adafruit-circuitpython-servokit>=1.3.0      # PCA9685 + 서보

# 센서
adafruit-circuitpython-dht>=4.0.0
pyserial>=3.5                          # mmWave UART

# 카메라
# picamera2는 pip로 깔면 libcamera 바인딩이 누락됨. apt로만 설치:
#   sudo apt install -y python3-picamera2 python3-libcamera
# venv는 반드시 --system-site-packages 옵션으로 만들어 apt 패키지가 보이게 해야 함.
opencv-python>=4.9.0
munkres>=1.1.4                         # IMX500 pose postprocess (Hungarian matching)
# mediapipe>=0.10.0                    # 손 제스처 인식 — Python 3.12 이하만 wheel 제공.
# Python 3.13+ 에서는 install 실패. 코드는 mediapipe 없으면 자동 skip.
# Python 3.12로 venv 만들면 활성화 가능.

# 음성 (USB 스피커폰)
sounddevice>=0.4.6                     # 마이크 캡처 + 재생
webrtcvad>=2.0.10                      # VAD (발화 구간 감지)
pvporcupine>=3.0.0                     # 웨이크 워드
openai>=1.30.0                         # Whisper STT + TTS
pydub>=0.25.1                          # MP3 디코딩 (TTS 출력 → PCM)
# ffmpeg 시스템 패키지 필요: sudo apt install -y ffmpeg

# AI (선택, 무거움) — 로컬 STT/TTS 대안
# whisper-cpp-python>=0.1.0            # 로컬 STT
# piper-tts>=1.2.0                     # 로컬 TTS
